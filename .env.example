# Copy to .env and fill in. NEVER commit .env.
CMC_MCP_API_KEY=            # from pro.coinmarketcap.com dashboard; sent as X-CMC-MCP-API-KEY
CLICKHOUSE_URL=http://localhost:8123   # optional: only needed to re-export data/lab/
